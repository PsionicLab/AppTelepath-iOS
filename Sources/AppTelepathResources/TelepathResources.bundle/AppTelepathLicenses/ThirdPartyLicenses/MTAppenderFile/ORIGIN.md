# MTAppenderFile 来源与修改

- 上游：https://github.com/meitu/MTAppenderFile
- 上游作者：Meitu, Inc.；其中 `comm/` 与 `loglib/mtaf_log_buffer.*` 派生自 Tencent Mars xlog，保留 THL A29 Limited 版权。
- 许可：MIT
- 精确取用版本：历史内嵌时未保留，当前无法从仓库事实恢复；不能因当前上游 tag 或文件相似度反推取用版本。
- 可核实修改：仓库只保留 SDK 使用的日志写入相关代码；随附 README 明确记录 `wwwcg` 增加压缩能力。
- 其他修改：更早的完整修改历史未保留，不作推定。

发行包内的 `LICENSE` 同时保留 Meitu 与 THL A29 Limited 的 MIT 版权声明。
